

export const config = {
  host: "localhost",
  port: 5432,
  user: "postgres",
  password: 'super',
  database: "postgres",
  connection_limit: 100
}

// https://www.postgresql.org/download/windows/